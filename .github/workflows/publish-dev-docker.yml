name: dev.docker build and release on Github

on:
  workflow_call:
    inputs:
      docker-file:
        description: Location and name of Dockerfile
        required: false
        type: string
        default: docker/dev.Dockerfile

jobs:
  docker-build-publish:
    runs-on: ubuntu-latest
    env:
      REPOSITORY-NAME: ${{ github.event.repository.name }}
      ORG-NAME: felleslosninger
      CONTAINER-REGISTERY: ghcr.io
      GIT_PACKAGE_USERNAME: ${{ secrets.MAVEN_USER }}
      GIT_PACKAGE_TOKEN: ${{ secrets.MAVEN_PASSWORD }}
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{env.CONTAINER-REGISTERY}}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            GIT_PACKAGE_USERNAME=${{env.GIT_PACKAGE_USERNAME}}
            GIT_PACKAGE_TOKEN=${{env.GIT_PACKAGE_TOKEN}}
          file: ${{ inputs.image-name }}
          labels: dev
          tags: ${{env.CONTAINER-REGISTERY}}/${{ORG-NAME}}/${{env.REPOSITORY-NAME}}:latest
          
